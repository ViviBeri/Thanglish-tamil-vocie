# Add these lines under "dependencies:" in app/pubspec.yaml
# (keep the existing "flutter: sdk: flutter" line that's already there)

  http: ^1.2.0
  just_audio: ^0.9.36
  path_provider: ^2.1.1
  dio: ^5.4.0
